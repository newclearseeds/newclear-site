Working on make quick site, nothing special yet, just testing alternative coding and looking how far this will go.

## Darts CV Prototype

Open `darts-cv-prototype.html` in a browser over `http://localhost` (camera access will not work on `file://` in most browsers).
